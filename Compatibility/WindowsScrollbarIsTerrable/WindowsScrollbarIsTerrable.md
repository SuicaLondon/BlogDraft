# How to use vw on Windows
**vw** and **vh** is a useful properity to limit the container size, However it will calculate with the scrollbar on Windows.

In macOS, all browser display scrollbar hovering.